apiVersion: v1
kind: Service
metadata:
  name: {{ template "spotlight-2-latest.fullname" . }}
  labels:
    app: {{ template "spotlight-2-latest.name" . }}
    chart: {{ template "spotlight-2-latest.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "spotlight-2-latest.name" . }}
    release: {{ .Release.Name }}
